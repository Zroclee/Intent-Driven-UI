## 2026-01-06  周二

当前进度：
1. 完成后端基础ReAct智能体服务，基于LangGraph实现流式响应。
2. 前端测试调用SSE请求成功

目标：
2026-01-08 晚上完成所有功能。

目标功能说明：
1. ReAct智能体，添加自定义车辆查询工具，定制化数据。
2. 前端在获取到MCP返回数据后渲染按钮。
3. 点击按钮将MCP数据解析动态加载对应组件，将数据复制给对应组件。

## 2026-01-07  周三

目标：
1. 必须完成自定义工具 2 个： 车辆列表、车辆定位。有时间添加轨迹服务。
2. 前端渲染工具返回数据。

## 2026-01-08  周四

车辆地图组件
车辆轨迹组件开发

## 2026-01-09  周五

如何映射到侧边栏并加载参数

演示


## 2026-01-12 周一

本周计划：
1. 完善细节，
2. 对话过程中动态加载组件的能力。
   

逻辑描述：
1. 接收到信息工具调用信息后，显示右侧侧边栏
2. 根据工具信息，动态加载组件。
3. 组件加载完成后，根据工具信息，动态加载参数。

# 下阶段开发 - 上下文工程

1. 构建一个lib项目, pnpm workspace 管理项目 monorepo ，包含原子化组件和预构建组件。
2. 完善业务组件加载过程，包括组件注册、参数传递、事件监听等。
3. 添加一个新的智能体来处理业务组件的注册和调用。
4. 优先完成多智能体的技术探索。

原子化组件和预构建组件

原子化组件：所有公共解耦拆分，以最小原子化产出组件。方便用户调用

预构建组件：一个组件封装所有功能，通过注册业务组件、emit等方式一键集成。

## 2026-01-16 周五

要完成多智能体1-2个模式的技术原型。

正在进行智慧农业智能体搭建

多智能体架构设计的重点 - 上下文管理

如何做好上下文管理？

!!!*** 其实前端并不局限于写死的业务组件，可以将原子化的组件教给AI训练，让AI自行组装页面 ***!!!

当然可能很多封装业务组件给AI识别是比较麻烦的事情
但是让AI直接生成HTML又比较浪费时间

下阶段的生成方式可能会是原子化的组件，AI来组装。训练AI掌握Element plus  Echart等组件
这个不会花费太多时间，而且页面效果和数据内容也在掌控

大致要分为三个阶段
1. 单业务场景、单智能体阶段。
2. 主管智能体和多个业务场景智能体。
3. 页面拼装阶段，智能体掌握根据数据组装页面的能力。


# 终极发现动态构建智能体
如果我们引入过多的业务场景，比如当前AI应用系统存在几十个业务场景。

创建几十个智能体肯定是不理智的。

动态创建智能体成为可能

需要一个组装智能体来负责组装任务智能体

动态生成并执行任务。


## 2026-01-20 周二

完成农业智能体和对应前端业务组件

后端服务准备多智能协作


## 2026-01-23

后端完成多智能体协作。

多智能体流式返回和业务组件动态加载


## 后续待完成工作
1. 记忆能力，支持多轮对话
2. 新增一个台账智能体，处理通用的台账能力。
3. 子智能体节点现在的写法不方便扩展，改为动态创建子智能体。
4. 接入一下向量数据库，用于后续长期记忆存储

## 2026-01-27 周二

今天需要验证一下记忆能力
添加对话滑动窗口，只保留近5轮对话内容。

尝试接入Milvus向量数据库，用于存储长期记忆。


## 2026-01-29 周四

已完成通过langgraph自带的工具完成记忆能力和多轮对话

下一步规划：

1. 多智能体路由模式存在瓶颈，后续改为skills多智能体模式（优先实施） 或者 单智能体+文件系统（claude skills体系 技术存疑，先保留）
2. 公共组件抽象-将聊天组件抽象出来，输出高度集成的IDChat和支持自定义的Bubble、Header、Input、Markdown-it 和最重要的DynamitComponentsRenderer
3. 暂停数据库存储、向量数据库、RAG等方式的记忆系统打造。寻求一键集成的方案


## 2026-01-30 周五

又有新的想法，既然全部交给AI管理，何不扩大一下范围，

页面也接入进来
页面、业务组件等所有前端静态资源全部移交AI控制

借助一些微前端的概念

将页面加载、组件渲染原子化

一个比你了解系统业务的AI助手

经过思考有两套体系
一、提供公共组件给到业务系统，自行组装智能体和配置业务组件
二、提供智能体服务和微前端接入，通过已部署好的AI应用配置化接入各种业务系统。


## 产品化方案

业务助手
- 如何快速接入？
- 提供后端接口，提供提示词配置、工具配置
- 业务组件要求

产品助手


## 2026-02-06 周五
项目全面调整成monorepo, 通过turbo+pnpm workspace 管理所有依赖和启动脚本。
开始产品助手的原型实现

设计几个假页面
- 用户管理 - 完成
- 角色管理 - 完成
- 数据台账 - 完成
- 统计报表 - 完成
- 应用管理 - 完成

## 2026-02-09 周一
完成远程组件功能验证
1. 创建远程组件项目
2. 打包三个远程组件，并生成UMD.js文件
3. 创建Express项目，将UMD文件通过接口返回。
4. 在express项目生成，远程组件的数据接口。‘

后续待完成功能：
1. 实现远程组件+业务接口数据的配置化


## 2026-02-10 周二
已经完成页面控制的产品助手功能实现。
下一步还缺少打开弹窗的功能，完成打开弹窗的功能即可向领导演示。

已经设计好了异步、串行的控制引擎
https://chatgpt.com/c/698c2dd0-f2e4-8321-83eb-02137c78b790
待实现

## 2026-02-24 周二

如何让ai可以驱动前端一些事件？
ai-action

```markdown
## 背景：
我正在开发一个有大模型驱动的业务系统，
系统需要根据用户输入的指令，调用前端的一些事件，比如跳转到某个页面、打开弹窗、填充表单数据、刷新页面等。
目前已完成后端智能体的开发，已完成前端页面和组件的开发。

## 需求：
1. 需要根据智能体返回的指令，调用前端的一些事件。
2. 需要支持前端事件的注册和调用。
3. 需要支持事件参数传递，用户可以在指令中传递参数给事件。
4. 智能体返回的是一个actions的数组需要支持串行调用，即一个事件调用完成后，才会调用下一个事件。

## 实现方案：
1. 需要有一个事件总线（event bus），用于前端事件的注册、调用、传递参数。
2. 因为串行调用事件总线的事件执行时使用Promise（配合 async/await），同时事件注册时需要提供完成的通知，用于在事件调用完成后，通知事件总线继续调用下一个事件。
3. 上述功能要作为三方库提供给其他系统使用，需要一个统一的文件来管理事件总线。同时使用时我希望在useAction等类似hooks的方法获取全局唯一的事件总线实例。

## 示例：
新增一个用户，用户名：李照鹏，组织：市场部，角色：普通用户
```

## 2026-2-27 周五
目前的产品AI助手对于原项目的改动较重，如何某个操作复杂的话，需要注册大量事件，不方便推广

后续打算研究无侵入性的操作方式。

目前有两种方案：
一种是基于MCP的通用浏览器服务（CDP/浏览器扩展）
另一种是：Gemini computer Use 基于视觉大模型分析截图，同时辅助DOM信息的方式操作。

第二种成本较高，先使用第一种方式实现

第一种方案的技术实现：
基于LangChain + Playwright


## 2026-3-12 周四
进度更新：已经完成了基于LangChain + Playwright的浏览器操作工具。 
目前还需要输入详细描述操作过程，只能根据文字完成基础操作。
后续步骤：
1. 将详细的操作步骤写入skill，用户只需要输入简单的指令，大模型去skill获取详细操作步骤，从而完成复杂的操作。
2. 探索模式开发，基于som（set of mark）技术，给页面元素添加标记，同时记录下元素的属性和位置信息。最好截图发给多模态大模型分析。然后再生成操作指令。

## 2026-3-13 周五
以此延伸出3个产品概念

- 自动化测试
- 页面性能分析
- 产品智能助手  

自动化测试

原子化工具、涌现能力、学习能力

给出一段操作视频，让大模型学习用户操作，并且生成操作指令。保存到本地skill

能自我进化的产品智能助手

学习，沉淀，操作

midscenejs 启发[文章](https://developer.aliyun.com/article/1687800)
既然选择了playwright，需要关注相关文章:
[Closer to the Metal: Leaving Playwright for CDP](https://browser-use.com/posts/playwright-to-cdp)
[微软 Playwright 也被革命了？Browser Use 最新版改用Chrome DevTools协议，更高效，更安全！](https://developer.volcengine.com/articles/7542490011762556971)



## 2026-3-16 周一
经过周末的学习
可以通用AI系统的的产品想法进行了升级

智能体不再和某个产品做绑定
而是指生产一个通用的智能体，用户可以根据自己的产品需求，配置不同的技能。
用户可以知道智能体形成skill
或者探索发现后生成skill
后续可以通过这些skill一键执行。


参考 阿里的[page-agent](https://github.com/alibaba/page-agent)
通过给页面注入脚本，读取页面可交互dom，创建一个可交互标记层，上面模拟鼠标点击网页，实现对页面的控制。
继续优化影子dom


我还是坚持自己的方案：
本地部署，基于playwright 操作浏览器，实现对页面的控制。
可以沉淀用户习惯。

## 2026-3-17 周二
已完成browser-agent的基础原型demo
效果令人满意，智能体很好的识别用户操作，并通过som标记和DOM上下文信息，准确的完成了操作
并且，基于文件操作的长期记忆和技能也能好的记录和使用。
堪称阶段性成果。

！！！重要下一阶段：
此功能可以作为一个独立产品。代码迁移至https://github.com/Zroclee/browser-agent

后续需要完善的功能：
1. 用户对话记录 （json）
2. 大模型apikey的配置化
3. 全局环境变量实现。能安装到本地全局环境变量

## 2026-3-18 周三
新的想法

将browser-agent迁移到新项目，起名 browserclaw

## 2026-3-20 周五

改造AI驱动组件的智能体

工具简化成1个，ai传入参数调用不同的接口返回信息

参数内容转化成skills，后续可以根据skills执行不同的操作

比如车的业务场景下，智能体判定需要车的skill，读取技能然后调用工具，传入对应参数，完成操作。


## 2026-3-23 周一

意图识别中台化改造

提供组件配置
用户可以创建平台/系统/应用，用户可以根据自己的需求配置提示词和对应接口，同时给接口绑定对应的组件。

如果现有组件不满足需求，也可以自建组件，通过远程组件的方式接近来。

行内肯定有很多类似一户一策的场景，定制化开发浪费时间人力，一个中台项目
用户可以根据需求自行开发接口、提示词、组件，组装智能体。